//经过一趟遍历让线性表变成奇数在前，偶数在后
//设置双指针，i从前往后遍历，j从后往前遍历，i发现一个偶数、j发现一个奇数就交换，直到i>j
void move(ElemType A[],int len){
	//初始化左右端下标
	int i=0,j=len-1;
	while(i<j){
		//从前往后找到偶数元素则停下，是奇数就跳过 
		while(i<j&&[i]%2!=0) i++;
		//从后往前经过奇数元素就停下，是偶数就跳过 
		while(i<j&&[j]%2!=1) j--;
		//交换他们
		if(i<j){
			swap(A[i],A[j]);
			//移动
			i++;j--;
		} 
	} 
} 
